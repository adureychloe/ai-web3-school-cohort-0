# 2026-06-09 学习笔记

> Agent Commerce Hub v0.5 — AI 意图匹配 + x402 双向资金流 + 完整端到端 Demo
> 距 Hackathon 截止还有 4 天

---

## 今日目标

1. 意图匹配从关键词硬匹配 → AI 语义匹配（DeepSeek API）
2. 实现 x402 协议：Agent 当卖家（HTTP 402 Payment Required）
3. Agent 当买家自动支付（x402 Client）
4. x402 Web UI 集成
5. 双端 Demo：同一个 Agent 既能花钱也能赚钱

## 完成事项

### 1️⃣ AI 意图匹配（Procurement Agent 升级）

之前：关键词硬匹配（`_CN_TAGS` + 单字分割）

现在：**DeepSeek V4 Flash 官方 API** 语义匹配 + 本地回退

```
用户输入 → DeepSeek API 分析 → JSON 匹配结果
                                   ↓
                    API 不可用时 → _local_score()
                                   语义组 + SequenceMatcher
```

| 查询 | 匹配结果 | 准确度 |
|------|----------|--------|
| "ETH 市场深度分析" | Premium Market Analyzer ✅ | AI 4/4 |
| "帮写一个Web3调研报告" | Research Notes Generator ✅ | AI 4/4 |
| "查一下这个钱包地址" | On-chain Data Fetcher ✅ | AI 4/4 |
| "最新比特币链上数据" | On-chain Data Fetcher ✅ | AI 4/4 |
| "分析NFT市场的趋势报告" | Premium Market Analyzer ✅ | AI 3/3 |
| "找个代写笔记的服务" | Research Notes Generator ✅ | AI 3/3 |

**零错误，6/6 全部正确**（中英文混合查询通过）

关键设计：`_local_score()` 基于语义组（write/data/analyze）+ SequenceMatcher，保证 DeepSeek API 503 时服务不中断。

### 2️⃣ x402 协议 — Agent 当卖家 💰

全新概念：**Agent 不止会花钱，还能赚钱。**

```
买方 Agent                卖方 Agent             链上
    │                        │                    │
    ├─── POST /api/x402/request ──►              │
    │                        │                    │
    │◄─── 402 Payment Required                   │
    │     X-Payment-Info:                        │
    │     chain=sepolia, token=SETH,             │
    │     amount=0.00006, addr=0x...             │
    │                        │                    │
    ├─── CAW Pact submit ──────►                  │
    ├─── CAW tx transfer ──────►                  │
    │◄─── content (delivery)                      │
```

新增文件：
- `x402_server.py` — FastAPI server，接收请求 → 返回 402 头 → 验证付款 → 交付内容
- `x402_client.py` — 自动检测 402 → 创建 Pact → 轮询审批 → 转账 → 重试 → 收货

### 3️⃣ Agent 当买家 — 自动支付 (x402 Client)

```
x402_client.request(service_id, query)
  → POST /api/x402/request
  ← HTTP 402 + X-Payment-Info header
  → CAW Pact submit (后台 polling)
  → 用户在 CAW App 审批
  → CAW tx transfer (自动)
  → 重试 GET with X-Tx-Hash
  ← 200 OK + content
```

**关键修复**：Pact 提交后等待审批超时 → 改为 `background=True` + `notify_on_complete`，审批完成后自动继续付款流程。两轮测试通过：

| 轮次 | 模式 | 结果 |
|------|------|------|
| 1st | 半自动（手动执行 transfer） | ✅ 成功付款 + 收货 |
| 2nd | 全自动（background polling） | ✅ 自动审批检测 + 自动付款 |

### 4️⃣ x402 CLI + Web UI

**CLI 新增命令：**

| 命令 | 功能 |
|------|------|
| `run.py serve` | 启动 x402 卖家服务器 |
| `run.py request <service_id> [query]` | 买方请求（含自动支付） |
| `run.py revenue` | 查看总营收 |
| `run.py list-services` | 查看所有 x402 服务 |

**Web UI 新增 "x402 Market" 标签页：**
- 卖家服务卡片列表（名称 + 价格 + 描述）
- 买家输入表单（选服务 + 写需求 → 自动支付）
- 营收仪表盘（总 SETH + USD 估算）

### 5️⃣ 端到端 Demo 验证

两轮完整双向 Demo：

```
Round 1: 手动流程
  Agent Commerce Hub (买方)
    → 发现卖方 x402 服务
    → HTTP 402 → CAW Pact → 用户审批 → Transfer → 收货
    
Round 2: 全自动流程
  Agent Commerce Hub (买方)
    → 请求服务 → HTTP 402 → CAW Pact (background)
    → notify_on_complete → 自动执行 transfer
    → 验证 tx_hash → 收货 ✅
```

### 6️⃣ CAW 扮演的角色（核心认知）

```
私钥钱包 (EOA) = 老板 👑     CAW 钱包 = 店长 🧑‍💼
├── 部署合约                   ├── 日常付款
├── 合约 Owner                 ├── 调用合约 (tx call)
└── 管理员权限                 ├── 收钱进钱包
                               ├── 受 Pact Policy 约束
                               └── 每笔需用户 App 审批

关系：两个独立账户，私钥钱包是合约 Owner，CAW 是操作用户。
     CAW 解决了"Agent 没私钥怎么付钱"的核心矛盾。
```

CAW = **项目唯一的资金通路**。没有 CAW，Agent 无法自动完成任何链上操作。

## 关键技术决策

| 决策 | 理由 |
|------|------|
| **DeepSeek 官方 API** (`api.deepseek.com`) | 之前试过 vpsairobot 代理不稳定且不恰当，换正式 API |
| **DeepSeek 回退到本地匹配** | API 503 时自动回退 `_local_score()`，不硬失败 |
| **x402 简单协议设计** | HTTP 402 + `X-Payment-Info` header 即可，不需要额外标准 |
| **background polling 模式** | Pact 等待审批约 10-30 秒，同步阻塞会超时 |
| **不重部署 ServiceRegistry** | 保留已有数据和存证 |

## 创新点评估

| 方向 | 状态 | 说明 |
|------|------|------|
| Agent-Native Payments (方向01) | ✅ 完成 | x402 自动支付 |
| Agent 当卖家 (创新缺口) | ✅ 完成 | 同一 Agent 收支双向 |
| A2A Economy (方向05) | ✅ 雏形 | 双向资金流 Demo 跑通 |
| AI 意图支付 | ✅ 完成 | DeepSeek 语义匹配服务 |

## 项目状态总览

```
v0.5 · 12 commits today · Sepolia 链上运行中

┌─────────────────────────────────────────────┐
│         Agent Commerce Hub v0.5              │
│                                              │
│  入口: CLI (run.py) + Web UI (:8080)         │
│  合约: ServiceRegistry @ Sepolia             │
│  钱包: CAW ✅ 已配对 · 余额 0.0085 SETH      │
│  服务: 3 链上 + 2 x402                       │
│  营收: 0.00013 SETH ($0.39) — 3 笔交易      │
│  存证: 2 笔链上交付记录                      │
│  匹配: DeepSeek AI + 本地回退                │
│  x402: HTTP 402 双向支付协议                  │
└─────────────────────────────────────────────┘
```

## 待办

- [ ] Demo 视频脚本（3-5 分钟）
- [ ] Demo 录制 + 剪裁
- [ ] Week 4 WCB 打卡提交
- [ ] 撤销未使用的 active Pact（CAW App）
- [ ] RISK_BOUNDARY.md（可选加分）
- [ ] 前端 UX 打磨

## 今日关键词

| 术语 | 含义 | 为什么重要 |
|------|------|-----------|
| **x402** | 基于 HTTP 402 Payment Required 的自动支付协议 | 让 Agent 能当卖家，不需要 API Key 就能收款 |
| **Pact** | CAW 的授权合约（限链/币种/金额/地址） | 让 Agent 能动钱，用户可控，安全边界 |
| **Intent Matching** | AI 理解用户意图并匹配到最合适的服务 | 从关键词硬匹配→语义理解，产品体验质变 |
| **A2A Economy** | Agent-to-Agent 经济 | Agent 间自动交易，是 Web3 AI 的核心叙事 |
